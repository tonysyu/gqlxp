## 1. Implementation

- [ ] 1.1 Update `app` command to use `--schema/-s` and `--default/-d` flags
- [ ] 1.2 Update `search` command to use `--schema/-s` and `--default/-d` flags
- [ ] 1.3 Update `show` command to use `--schema/-s` and `--default/-d` flags
- [ ] 1.4 Update `resolveSchemaFromArgument` to handle flag-based schema resolution
- [ ] 1.5 Update command help text and examples

## 2. Testing

- [ ] 2.1 Test app command with `--schema` flag
- [ ] 2.2 Test app command with `--default` flag
- [ ] 2.3 Test search command with `--schema` flag
- [ ] 2.4 Test search command with `--default` flag
- [ ] 2.5 Test show command with `--schema` flag
- [ ] 2.6 Test show command with `--default` flag
- [ ] 2.7 Verify error handling when no schema specified

## 3. Documentation

- [ ] 3.1 Update README examples
- [ ] 3.2 Update documentation to reflect new flag usage
